; Hoist framework dependencies to the project root so IDEs (Volar) can
; resolve types for `<CodeBlock>`, `<Container>`, etc. when using pnpm.
; Safe to keep with npm/yarn — they just emit one warning and ignore it.
shamefully-hoist=true
